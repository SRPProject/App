import {BrowserRouter} from "react-router-dom"
import {useState} from "react"

function App() {

  const [isAuth, changeAuth] = useState<Boolean>(false);
  const [load,changeLoad] = usSatet

  return (
    <div className="App">
      
    </div>
  );
}

export default App;
